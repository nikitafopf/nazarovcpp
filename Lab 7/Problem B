#include <iostream>
using namespace std;

void check(int b){
    if((b % 4 == 0)&&(b / 1000 != 4)&&(b / 1000 != 5))
        cout << b << endl;
    else if((b % 7 == 0)&&(b / 1000 != 7)&&(b / 1000 != 1))
        cout << b << endl;
    else if((b % 9 == 0)&&(b / 1000 != 9)&&(b / 1000 != 8))
        cout << b << endl;
}
int main(){
    int N;
    cin >> N;
    int a[N];
    for (int i=0; i < N;  i++){
        cin >> a[i];
        }
    for (int i=0; i < N;  i++){
        check(a[i]);
        }
    return 0;
    }
